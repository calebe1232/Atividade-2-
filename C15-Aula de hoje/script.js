let mensagem;
if (nota >= 6) {
    mensagem = 'Aprovado';
    } else {
        mensagem = 'Reprovado'
    }

const mensagem= nota >= 6 ? 'Aprovado' : 'Rreprovado';

let descricao = temperatura >= 30 ? 'Quente' : 'Agradável';

let tipo = numero % 2 === 0 ? 'par' : 'ímpar';

let saudacao = hora =< 12 ? 'Bom dia' : 'Boa tarde/noite';

